spring:
  rabbitmq:
    host: 192.168.80.139
    username: admin
    password: 1111
    virtual-host: /

management:
  endpoint:
  endpoints:
    web:
      exposure:
        include: bus-refresh #springboot 默认开启了权限拦截 会导致/bus-refresh出现 405，拒绝访问